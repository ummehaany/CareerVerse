/*
 * Superseded — Career Discovery v2 no longer scores against a hand-picked
 * career list. It scores every career in the existing `lib/careers/catalog.ts`
 * (119 careers, 15 categories) via the field taxonomy in `fields.ts` and the
 * trait vectors derived in `traits.ts`. See `scoring.ts` for the engine.
 *
 * This file is intentionally left empty (the sandbox this was built in
 * doesn't allow deleting files from the mounted project folder) and nothing
 * imports it anymore.
 */
export {};
