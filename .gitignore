# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
build.log
dev.log

# local scratch / personal diagnostic data — not for the repo.
# The reusable scripts/*.ts diagnostics themselves ARE committed; only their
# throwaway scratch files and generated per-user output are excluded here.
/__debug_find_user.mjs
scripts/__tmp_*
scripts/my-discovery-answers.json

# session investigation/report logs — first-person AI-assistant working
# notes from local sessions, not maintained project documentation. The
# durable "why" behind each change already lives in code comments (see
# scoring.ts, traits.ts, normalize.ts, recommendation-fallback.ts, etc.).
# Delete this block (and `git add` the files) if you'd rather keep them as
# a decision-log, matching the two similar reports already committed.
/CareerVerse_Career_Assessment_Implementation_2026-08-15.md
/CareerVerse_Career_Assessment_Investigation_2026-08-15.md
/CareerVerse_Firestore_Index_Fix_2026-08-15.md
/CareerVerse_Formula_C_Implementation_2026-08-15.md
/CareerVerse_Scoring_Clustering_Investigation_2026-08-15.md
/CareerVerse_Scoring_Formula_Comparison_2026-08-15.md

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
